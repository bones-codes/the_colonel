#!/bin/bash

cd lkm/

# remove keylogger
echo -n sp >> /proc/colonel
echo -n sp >> /proc/colonel
sudo killall col_kl
sudo rm col_kl

# cleanup the rootkit
echo -n ms >> /proc/colonel
sudo rmmod rootkit
sudo make clean

# remove logs
sudo rm -rf /opt/__col_log
